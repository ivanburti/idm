<?php
// module/Access/view/access/access/orphans.phtml
$title = $this->translate('Orphans');
$this->headTitle($title);
?>

<table class="table table-striped">
    <thead>
        <tr>
            <th></th>
            <th><?= $this->translate('Resource') ?></th>
            <th><?= $this->translate('Username') ?></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($accesses as $access) : ?>
            <tr>
                <td>
                    <?php if ($access->isEnabled()) : ?>
                        <span class="badge badge-success"><?= $this->translate('Enabled') ?></span>
                    <?php endif; ?>
                </td>
                <td><?= $this->escapeHtml($access->getResourceName()) ?></td>
                <td><?= $this->escapeHtml($access->getUsername()) ?></td>
                <td>
                    <?php if (1 == 1): ?>
                        <a class="btn btn-sm btn-primary" href="<?= $this->url('access', ['action' => 'link', 'id' => $access->getAccessId()]) ?>"><i class="fas fa-link"></i></a>
                    <?php endif; ?>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
